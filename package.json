{
  "name": "react-util-hooks",
  "private": true,
  "license": "MIT",
  "repository": "https://github.com/bikk-uk/react-util-hooks",
  "workspaces": [
    "packages/use-progressive-list"
  ],
  "scripts": {
    "test": "yarn workspaces foreach test",
    "test-watch": "yarn workspaces foreach test-watch",
    "test-watch-all": "yarn workspaces foreach test-watch-all",
    "lint": "yarn workspaces foreach lint",
    "build": "yarn workspaces foreach run build",
    "clean": "yarn workspaces foreach test-watch-all",
    "release": "yarn workspaces foreach test-watch-all",
    "lerna": "lerna"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
